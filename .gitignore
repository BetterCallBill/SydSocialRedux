# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables (contains the Firebase web API key) - see .env.example
.env
.env.local
.env.production
.env.development

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Firebase CLI local state (not used for deployment; Auth/Firestore/Storage config is in src/app/config/firebase.ts)
.firebase

# Claude Code local config
.claude

# Personal notes, not part of the shipped repo
interview_prep.md
